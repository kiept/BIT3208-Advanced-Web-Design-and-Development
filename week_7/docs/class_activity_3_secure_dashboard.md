## Class Activity 3 — Secure a Dashboard

Requirements:
- Allow only logged-in users to view the dashboard.
- Display user details and a logout option.

Implementation pointers:
- Use `require_login()` and `require_role()` as needed.
- Display `fullname` and `email` from `current_user()`.

Deliverables:
- Screenshot of the dashboard showing logged-in user's info.
